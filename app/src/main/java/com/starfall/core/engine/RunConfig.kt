package com.starfall.core.engine

/** Tunable run-level configuration for dungeon length and rewards. */
object RunConfig {
    const val MAX_FLOOR = 20
    const val FINAL_BOSS_ID = "titan_heart"

    const val META_CURRENCY_PER_FLOOR = 8
    const val META_CURRENCY_PER_BOSS = 25
    const val META_CURRENCY_PER_ELITE = 4
    const val VICTORY_META_MULTIPLIER = 1.5
}
